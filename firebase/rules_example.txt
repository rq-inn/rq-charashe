{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null && $roomId.matches(/^[A-Za-z0-9_-]{3,32}$/)",
        "sharedText": {
          ".validate": "newData.hasChildren(['value', 'updatedAt', 'updatedBy'])",
          "value": {
            ".validate": "newData.isString() && newData.val().length <= 5000"
          },
          "updatedAt": {
            ".validate": "newData.isNumber()"
          },
          "updatedBy": {
            ".validate": "newData.isString()"
          }
        }
      }
    }
  }
}
